import React from 'react'
import { Layout } from 'antd'

// import "./App.scss";

function SiteFooter() {
  const { Footer } = Layout

  return (
    <Footer className='container'>
      Build with ...
    </Footer>
  )
}

export default SiteFooter