import React from 'react'
import { Layout } from 'antd'

// import "./App.scss";

function SiteHeader() {
  const { Header } = Layout

  return (
    <Header className='container'>
      Header
    </Header>
  )
}

export default SiteHeader